{
  "$schema": "http://json-schema.org/schema",
  "id": "BBModuleSchematics",
  "title": "BB Module Options Schema",
  "type": "object",
  "description": "Creates an Angular module with BB naming conventions",
  "properties": {
    "path": {
      "type": "string",
      "description": "The path of the module",
      "$default": {
        "$source": "argv",
        "index": 0
      },
      "x-prompt": "What is the module's path? (e.g. feature/module-path/my-module)"
    },
    "baseDir": {
      "type": "string",
      "description": "The base directory for the module path"
    }
  },
  "required": [
    "path"
  ]
}
