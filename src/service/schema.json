{
  "$schema": "http://json-schema.org/schema",
  "id": "BBServiceSchematics",
  "title": "BB Service Options Schema",
  "type": "object",
  "description": "Creates an Angular service with BB naming conventions",
  "properties": {
    "path": {
      "type": "string",
      "description": "The path of the service",
      "$default": {
        "$source": "argv",
        "index": 0
      },
      "x-prompt": "What is the service's path? (e.g. service-path/my-service)"
    }
  },
  "required": [
    "path"
  ]
}
