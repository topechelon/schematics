{
  "$schema": "http://json-schema.org/schema",
  "id": "BBApiSchematics",
  "title": "BB API Options Schema",
  "type": "object",
  "description": "Creates a BB API Service and Model",
  "properties": {
    "path": {
      "type": "string",
      "description": "The path of the model",
      "$default": {
        "$source": "argv",
        "index": 0
      },
      "x-prompt": "What is the model's path? (e.g. network/discussion/comment)"
    }
  },
  "required": [
    "path"
  ]
}
